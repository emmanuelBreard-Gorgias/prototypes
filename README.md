# SA Prototypes

Internal design prototypes for Shopping Assistant. Not indexed — share via direct URL only.

## Structure

Each prototype lives in its own folder:

```
/search-entry-point/design-variants.html   — Search bar entry point variants
```
